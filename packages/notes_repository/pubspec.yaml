name: notes_repository
description: "A repo that handle notes"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  equatable: ^2.0.5
  firebase_auth: ^4.17.4
  cloud_firestore: ^4.15.4
  uuid: ^4.3.3
  flutter:
    sdk: flutter
  path_provider: ^2.1.3

dev_dependencies:
  isar_generator: ^3.1.0+1
  build_runner: any
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
